import classes from './styles.module.css';
import Router from '../../routes/Router';

function App() {
  return (
    <main className={classes.main}>
      <Router />
    </main>
  );
}

export default App;
